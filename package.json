{
	"name": "wrap-comments",
	"displayName": "Wrap comments",
	"publisher": "LewisHowles",
	"version": "0.2.1",
	"description": "A VS Code extension that reflows comments to a preferred line length, helping keep longer explanations readable and consistently formatted.",
	"main": "./extension.js",
	"icon": "assets/icon.png",
	"repository": {
		"type": "git",
		"url": "https://github.com/lewishowles/wrap-comments.git"
	},
	"contributes": {
		"commands": [
			{
				"command": "wrap-comments.wrapComment",
				"title": "Wrap comment"
			}
		],
		"configuration": {
			"properties": {
				"wrapComments.lineLength": {
					"type": "number",
					"default": 80,
					"description": "Line length to wrap comments to"
				}
			}
		}
	},
	"engines": {
		"vscode": "^1.92.0"
	},
	"scripts": {
		"test": "vitest"
	},
	"devDependencies": {
		"@stylistic/eslint-plugin": "^5.10.0",
		"@vitest/ui": "latest",
		"globals": "^17.5.0",
		"vitest": "^4.1.5"
	}
}
